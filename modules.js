const circleArea = (r) => {
  console.log('The circle area is => ', 3.14 * r ** 2);
};

const sqaureArea = (s) => {
  console.log('The square area is => ', s * s);
};

export { circleArea, sqaureArea };
